# =============================================================================
# MinIO Environment Configuration
# =============================================================================
# 1. Copy this file:    cp .env.example .env
# 2. Edit .env and fill in your values
# 3. NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Admin Credentials  ⚠ CHANGE BEFORE PRODUCTION
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_this_password

# -----------------------------------------------------------------------------
# Server URLs
# Development  → use your server IP:
#   MINIO_SERVER_URL=http://192.168.1.100:9000
#   MINIO_BROWSER_REDIRECT_URL=http://192.168.1.100:9001
#
# Production (with Nginx + HTTPS) → use your domains:
#   MINIO_SERVER_URL=https://storage.example.com
#   MINIO_BROWSER_REDIRECT_URL=https://console.example.com
# -----------------------------------------------------------------------------
MINIO_SERVER_URL=http://YOUR_SERVER_IP:9000
MINIO_BROWSER_REDIRECT_URL=http://YOUR_SERVER_IP:9001

# -----------------------------------------------------------------------------
# Ports (host machine — change if these are already in use)
# -----------------------------------------------------------------------------
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# -----------------------------------------------------------------------------
# Buckets to create on first startup (space-separated)
# -----------------------------------------------------------------------------
MINIO_BUCKETS=pctracking

# -----------------------------------------------------------------------------
# Lifecycle: auto-delete files after N days
# -----------------------------------------------------------------------------
MINIO_EXPIRE_DAYS=15

# -----------------------------------------------------------------------------
# Allow long-lived presigned URLs
# 2592h = 108 days
# -----------------------------------------------------------------------------
MINIO_MAX_PRESIGNED_TTL=2592h
